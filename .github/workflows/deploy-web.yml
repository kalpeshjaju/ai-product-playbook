##
# FILE PURPOSE: Deploy web app to Vercel on push to main
#
# WHY: Path-filtered deploys — only deploys when web app or shared packages change.
# HOW: Vercel CLI deploy. Stack TBD (placeholder until framework chosen).
#
# AUTHOR: Claude Opus 4.6
# LAST UPDATED: 2026-02-28
##

name: Deploy Web

on:
  push:
    branches: [main]
    paths:
      - 'apps/web/**'
      - 'packages/shared-types/**'
      - 'packages/shared-ui/**'
      - 'packages/shared-llm/**'

concurrency:
  group: deploy-web-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Uncomment when Vercel is configured:
    # environment: production
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - run: npm ci --legacy-peer-deps

      - name: Build shared packages
        run: npx turbo run build --filter=@playbook/web...

      # Uncomment when Vercel is configured:
      # - name: Deploy to Vercel
      #   run: npx vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
      #   working-directory: apps/web

      - name: Placeholder
        run: echo "Web deploy pipeline ready — configure Vercel when framework is chosen"
