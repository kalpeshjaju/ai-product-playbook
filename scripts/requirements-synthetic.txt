# Synthetic eval data generation dependencies
# Pinned for deterministic installs — update explicitly, not via resolver drift
gretel-client==0.18.3
psycopg2-binary==2.9.10
python-dotenv==1.0.1
pandas==2.2.3
