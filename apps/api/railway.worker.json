{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npx turbo run build --filter=@playbook/api"
  },
  "deploy": {
    "startCommand": "node apps/api/dist/worker.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
