# DSPy prompt optimization service dependencies
# Pinned for deterministic installs — update explicitly, not via resolver drift
dspy-ai==2.6.1
litellm==1.63.14
psycopg2-binary==2.9.10
python-dotenv==1.0.1
pyyaml==6.0.2
