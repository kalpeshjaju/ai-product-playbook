{
  "name": "@playbook/shared-types",
  "version": "0.1.0",
  "description": "Shared TypeScript types and interfaces across all apps",
  "type": "module",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "tsc",
    "lint": "eslint src/ --max-warnings 0",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
